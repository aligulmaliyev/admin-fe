services:
  api:
    image: admin-fe:latest
    container_name: admin-fe
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env  # Load environment variables from .env file if exists
